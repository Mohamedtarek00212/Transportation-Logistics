# SQL Source Tables

This directory contains the CSV files used by the SQL Server bulk-load section.

The database schema defines 13 relational tables. Twelve CSV source files are currently available here. `TrackingEvents.csv` is referenced by the final SQL script but is missing from the supplied project files and must be added before running the complete bulk load.

